import type { NextConfig } from "next";
// import path from "path";

const nextConfig: NextConfig = {
  /* config options here */
  // Temporarily disable Turbopack to avoid unexpected panics during dev.
  // Set to `false` to force Next.js to use the stable Webpack dev server.
  // turbopack: false,
};

export default nextConfig;
